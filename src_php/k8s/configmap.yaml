apiVersion: v1
kind: ConfigMap
metadata:
  name: multipersona-config
  namespace: default
data:
  APP_ENV: "production"
  LOG_LEVEL: "info"
  QDRANT_HOST: "qdrant-service"
  QDRANT_PORT: "6333"
  STORAGE_DIR: "/app/data"
