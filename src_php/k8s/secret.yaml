apiVersion: v1
kind: Secret
metadata:
  name: multipersona-secrets
  namespace: default
type: Opaque
stringData:
  # Replace these with actual base64 encoded values or use a secret manager
  MISTRAL_API_KEY: "your_mistral_api_key_here"
  QDRANT_API_KEY: "your_qdrant_api_key_here"
