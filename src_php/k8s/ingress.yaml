apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: multipersona-ingress
  namespace: default
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: multipersona.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: multipersona-service
            port:
              number: 80
